import React from 'react'

function ChartsSentences() {

    let chartFirstLine = `We generated a ${cTitle} Event of the last ${logs} days on ${log_date} from Apex central/Apex One. There was a total of ${total_detection} detections.`;


    return (
        <>


        </>
    )
}

export default ChartsSentences
